import { PopoverTrigger as PopoverTriggerChakra } from "@chakra-ui/react";

export function PopoverTrigger({ children }: { children: React.ReactNode }) {
  return <PopoverTriggerChakra>{children}</PopoverTriggerChakra>;
}
