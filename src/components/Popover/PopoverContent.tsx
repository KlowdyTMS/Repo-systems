import { PopoverContent as PopoverContentChakra } from "@chakra-ui/react";

export function PopoverContent({ children }: { children: React.ReactNode }) {
  return <PopoverContentChakra>{children}</PopoverContentChakra>;
}
